# Spring Boot Application Configuration
# CleanConfig will validate these values against the rules defined in ApplicationPropertiesConfig

# Server configuration
server.port=8080

# Application metadata
app.name=CleanConfig Spring Boot Example
app.admin.email=admin@cleanconfig.example.com

# Database configuration
app.database.url=jdbc:h2:mem:testdb
app.database.max-connections=20

# API configuration
app.api.key=production-api-key-1234567890abcdefgh

# CleanConfig settings (optional)
# cleanconfig.enabled=true
# cleanconfig.validation.fail-on-error=true
# cleanconfig.validation.log-warnings=true
