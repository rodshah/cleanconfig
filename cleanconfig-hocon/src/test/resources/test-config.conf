server {
  host = "localhost"
  port = 8080
}

database {
  url = "jdbc:postgresql://localhost:5432/test"
  pool-size = 10
}

features = ["auth", "logging", "metrics"]

app {
  name = "test-app"
  debug = true
  timeout = 30s
}

resolved-value = ${server.host}
